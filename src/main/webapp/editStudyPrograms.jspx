
<f:view 
	xmlns:f="http://java.sun.com/jsf/core"
	xmlns:h="http://java.sun.com/jsf/html"
	xmlns:ice="http://www.icesoft.com/icefaces/component">
	<f:loadBundle basename="Language" var="msgs" />
	<ice:portlet>
		<ice:form partialSubmit="true" >
			<ice:outputText value="Edit study program"/><br/>
			<br/>
			<ice:outputText value="Institute: "/> 
				<ice:selectOneMenu id="instituteInput" value="#{employeeService.selectedInstituteNumber}" valueChangeListener="#{employeeService.actionUpdateStudyProgramList}" partialSubmit="true">
					<f:selectItems id="instituteInputContent" value="#{employeeService.instituteList}"/>
				</ice:selectOneMenu>
			<ice:dataTable id="studyProgramTable" var="item" value="#{employeeService.studyProgramList}">
				<ice:column>
					<f:facet name="header">
						<ice:outputText value="Study programs:" rendered="#{!empty employeeService.selectedInstitute}"/>
					</f:facet>
					<ice:outputText id="itemName" value="#{item.label}" rendered="#{!item.editable}"/>
					<ice:inputText id="itemNameInput" value="#{item.label}" rendered="#{item.editable}"/>
					<ice:commandButton id="setEditable" value="Edit row" actionListener="#{editBean.actionSetEditable}" rendered="#{!empty item.label}"/>
					<ice:commandButton id="remove" value="Remove" actionListener="#{editBean.actionRemoveStudyProgram}" rendered="#{!empty item.label}"/>
				</ice:column>
			</ice:dataTable>
			<ice:commandButton id="addNewRow" value="Add row" actionListener="#{editBean.actionAddNewStudyProgram}" rendered="#{!empty employeeService.selectedInstitute}"/>
			<table border="0">
				<tr>
					<td></td><td></td>
					<td><ice:commandButton type="submit" action="editInformation" value="Back" id="BackButton" immediate="true"/></td>
				</tr>
			</table>
		</ice:form>
	</ice:portlet>
</f:view>